---
- name: Install Busy
  include_tasks: "{{ epics_build_module_task }}"
  vars:
    epics_modules_to_build: "{{ m_busy_modules }}"
  when: not (m_busy_installed | default(false))

- name: Mark m-busy as installed
  set_fact:
    m_busy_installed: true
