---
- name: "Build EPICS Modules"
  include_tasks: "{{ epics_build_single_module }}"
  loop: "{{ epics_modules_to_build }}"
  loop_control:
    loop_var: module
  when:
    - module.id != 'skip'
    - not (module.skip | default(false) | bool)
