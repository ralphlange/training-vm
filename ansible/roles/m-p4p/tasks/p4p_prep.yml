---
- name: "{{ module.name }} | Set python version"
  ansible.builtin.copy:
    dest: "{{ epics_install_path }}/{{ module.id }}-{{ module.version }}/configure/os/CONFIG_SITE.Common.linuxCommon"
    content: "PYTHON = python3"
  become: true
