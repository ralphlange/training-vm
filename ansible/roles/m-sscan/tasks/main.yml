---
- name: Install SScan
  include_tasks: "{{ epics_build_module_task }}"
  vars:
    epics_modules_to_build: "{{ m_sscan_modules }}"
  when: not (m_sscan_installed | default(false))

- name: Mark m-sscan as installed
  set_fact:
    m_sscan_installed: true
